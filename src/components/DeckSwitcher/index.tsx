// src/components/DeckSwitcher/index.tsx
//
// TopBar chip — shows the active deck's name + dirty pip + Save pill + caret.
// Click opens the Dropdown. Click the Save pill commits the active deck.
//
// Pure-helper logic for chip state and dropdown rows lives in `./helpers`.

export { default } from './Dropdown'
